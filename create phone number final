function createPhoneNumber(numbers){
let sectionOne = (`(`+numbers[0]+numbers[1]+numbers[2]+`)`);

let sectionTwo = (` `+numbers[3]+numbers[4]+numbers[5]);

let sectionThree = (`-`+numbers[6]+numbers[7]+numbers[8]+numbers[9]);
return sectionOne+sectionTwo+sectionThree;
}